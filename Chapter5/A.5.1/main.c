#include <stdio.h>
#include <stdlib.h>

int main()
{
    float a,b,c;
    printf("dose tis treis times : \n");
    scanf("%f%f%f",&a,&b,&c);
    (a>100) ? printf("einai\n") : printf("den einai\n");
    (b>100) ? printf("einai\n") : printf("den einai\n");
    (c>100) ? printf("einai\n") : printf("den einai\n");
    return 0;
}
