#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{
    int i,j,k,dis,len,cnt=1,z=0;
    char a[100];
    gets(a);
    len=strlen(a);
    /*Αυτή η άσκηση με δυσκόλεψε αρκετά αλλά είναι πλέον ολόσωστη*/
    if (5>2 && 2>1 && 3<9)
    printf("swstos");

    return 0;
}
